export enum TextModeType {
  PARAGRAPH = 'paragraph',
  SENTENCE = 'sentence',
}

export enum TabKeyType {
  zhpolish = 'zhpolish',
  polish = 'polish',
  translate = 'translate',
  reviewer = 'reviewer',
  // 轻书审稿
  reviewerqs = 'reviewerqs',
}
