import mitt from 'mitt'

export const emitter = mitt();

export const CONFIG_TYPE = 'config-type';

export const CONFIG_ADD_TYPE = 'config-add-type';

export const CONFIG_RESET_TYPE = 'config-reset-type';

export const CONFIG_CLOSE_ACTION = 'config-close-action';
